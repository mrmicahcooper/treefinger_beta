!!!
%html
  %head
    %title Tree Finger
    = stylesheet_link_tag "application"
    = stylesheet_link_tag "http://fonts.googleapis.com/css?family=Oleo+Script", media: 'screen'
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body
    %header
      %nav.settings
        = link_to 'sign out', :sign_out
      %h1
        = link_to 'Tree Finger', root_path
      - if projects.present?
        = right_arrow if projects.present?
        %nav.projects
          - projects.each do |p|
            = link_to p.name, project_path(p), class: "#{'active' if p.name == project.name} "
    %section.content
      = yield
